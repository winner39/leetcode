#pragma once
#include "dependencies.h"
class Solution {
public:
    bool isMatch(string s, string p) {
        vector<vector<bool> > result;
        if(p)
    }
};
void test() {
    Solution solution = {};
    cout << solution.isMatch("aaca", "ab*a*c*a");
}
void printint(int a) {
    cout << a << ",";
}